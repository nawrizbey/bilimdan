DATABASE_URL="postgresql://bilimdon:bilimdon@localhost:5432/bilimdon?schema=public"
JWT_SECRET="change-me-in-production"
PORT=4000
# Comma-separated list of allowed frontend origins. Defaults to the Vite dev
# server (http://localhost:5173) if unset — override in production.
CORS_ORIGIN="http://localhost:5173"
